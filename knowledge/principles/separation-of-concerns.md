# 关注点分离

每个文件都应放在符合其职责的目录中。

- `CLAUDE.md` 负责启动项目工作流。
- `.claude/rules/` 负责最小启动强制规则。
- `agents/` 负责执行编排和可复用能力。
- `references/wips/` 负责原始草案。
- `products/` 负责产品细化和故事工作。
- `knowledge/` 负责可复用权威知识。

不要把一个文件变成规则堆。只要一段内容形成了独立知识对象，并且有自己的生命周期，就应拆成子文件。
